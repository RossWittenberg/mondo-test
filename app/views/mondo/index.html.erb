<!-- Title Section -->
<%= render partial: 'title' %>

<!-- Screens Section -->
<%= render partial: 'screens' %>

<!-- Headphones Section -->
<%= render partial: 'headphones' %>

<!-- Features Section -->

<%= render partial: 'features' %>

<!-- Perspective Section -->
<%= render partial: 'perspective' %>


